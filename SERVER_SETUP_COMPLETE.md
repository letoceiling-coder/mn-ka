# Финальная настройка сервера после установки зависимостей

## ✅ Шаг 1: Настройте .env

Откройте `.env` на сервере и добавьте/обновите:

```env
PHP_PATH=/usr/local/bin/php8.2
```

Или используйте алиас:

```env
PHP_PATH=php8.2
```

(Если `/usr/local/bin` в PATH, то `php8.2` тоже сработает)

---

## ✅ Шаг 2: После завершения установки зависимостей

Выполните эти команды:

```bash
cd /home/d/dsc23ytp/stroy/public_html

# Используйте полный путь к PHP 8.2
PHP82="/usr/local/bin/php8.2"

# Очистите кеши
$PHP82 artisan optimize:clear
$PHP82 artisan config:clear
$PHP82 artisan route:clear
$PHP82 artisan cache:clear

# Обновите автозагрузку
$PHP82 /usr/local/bin/composer dump-autoload

# Или если composer в PATH:
# $PHP82 composer dump-autoload

# Выполните миграции (если были новые)
$PHP82 artisan migrate --force

# Оптимизируйте приложение
$PHP82 artisan config:cache
$PHP82 artisan route:cache
$PHP82 artisan view:cache
```

---

## ✅ Шаг 3: Проверьте настройки

```bash
# Проверьте версию PHP, которую использует Artisan
/usr/local/bin/php8.2 artisan --version

# Проверьте маршруты
/usr/local/bin/php8.2 artisan route:list | grep deploy

# Проверьте конфигурацию
/usr/local/bin/php8.2 artisan config:show | grep -E "PHP_PATH|DEPLOY_TOKEN"
```

---

## ✅ Шаг 4: Настройте веб-сервер (важно!)

Убедитесь, что веб-сервер использует PHP 8.2, а не PHP 5.6.

### Проверьте версию PHP для веб-сервера:

```bash
# Создайте тестовый файл
echo "<?php phpinfo(); ?>" > public/test.php
```

Откройте в браузере: `https://post-ads.ru/test.php`

Проверьте, что показывает версию PHP 8.2.x, а не 5.6.x

**Удалите тестовый файл после проверки:**
```bash
rm public/test.php
```

### Настройка Apache (если используется)

Если веб-сервер использует PHP 5.6, нужно обновить настройки. Обратитесь к администратору хостинга или настройте через панель управления.

---

## ✅ Шаг 5: Проверьте работу деплоя

После завершения всех настроек попробуйте деплой с локальной машины:

```bash
php artisan deploy --insecure
```

Или отправьте тестовый запрос:

```bash
curl -X POST https://post-ads.ru/api/deploy \
  -H "X-Deploy-Token: ваш-токен-из-env" \
  -H "Content-Type: application/json"
```

---

## 🔧 Альтернатива: Создайте алиас для удобства

Добавьте в `~/.bashrc`:

```bash
echo 'alias php82="/usr/local/bin/php8.2"' >> ~/.bashrc
source ~/.bashrc
```

Теперь можно использовать:
```bash
php82 artisan migrate
php82 composer install
```

---

## 📝 Краткая версия команд (после установки зависимостей)

```bash
cd /home/d/dsc23ytp/stroy/public_html
PHP82="/usr/local/bin/php8.2"

# 1. Добавьте в .env: PHP_PATH=/usr/local/bin/php8.2

# 2. Очистите и оптимизируйте
$PHP82 artisan optimize:clear
$PHP82 artisan config:clear
$PHP82 artisan route:clear
$PHP82 composer dump-autoload
$PHP82 artisan migrate --force
$PHP82 artisan config:cache
$PHP82 artisan route:cache
$PHP82 artisan view:cache

# 3. Проверьте
$PHP82 artisan route:list | grep deploy
```

