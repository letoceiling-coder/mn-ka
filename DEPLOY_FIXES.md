# Исправления для команды deploy

## ✅ Исправленные проблемы

### 1. Дублирование URL `/api/deploy/api/deploy`

**Проблема:** URL дублировался при формировании.

**Решение:** Улучшена логика формирования URL - теперь правильно удаляется дублирование пути.

**Проверьте `.env`:**
```env
SERVER_URL=https://post-ads.ru
```
Не используйте:
```env
SERVER_URL=https://post-ads.ru/api/deploy  # ❌ Неправильно
```

### 2. Таймаут git push

**Проблема:** `The process "git push origin main" exceeded the timeout of 60 seconds`

**Решение:** 
- Увеличен таймаут до 5 минут (300 секунд)
- Добавлена проверка больших файлов перед коммитом
- Улучшены сообщения об ошибках

### 3. Большие файлы в git (lagom-figma.rar)

**Проблема:** Архивы попадали в коммит.

**Решение:**
- Добавлены `.rar`, `.zip`, `.7z` в `.gitignore`
- Добавлена проверка больших файлов перед коммитом с предупреждением
- Файл `lagom-figma.rar` удален из git

---

## 📝 Что нужно сделать сейчас

### Шаг 1: Закоммить исправления

```bash
git add .gitignore app/Console/Commands/Deploy.php
git commit -m "Fix: исправлен URL, таймаут и обработка больших файлов"
```

### Шаг 2: Проверить изменения перед push

```bash
git status
```

Убедитесь, что:
- `.gitignore` изменен
- `Deploy.php` изменен
- `lagom-figma.rar` удален (статус `D`)

### Шаг 3: Отправить изменения

```bash
git push origin main
```

Если push всё ещё долгий из-за предыдущих коммитов с большими файлами, можно попробовать:

```bash
# Увеличить таймаут для git
git config http.postBuffer 524288000  # 500MB

# Или использовать SSH вместо HTTPS (если возможно)
```

### Шаг 4: После успешного push

Попробуйте деплой снова:

```bash
php artisan deploy --insecure
```

---

## 🔍 Проверка исправлений

### Проверка URL

В `.env` убедитесь:
```env
SERVER_URL=https://post-ads.ru
```

URL должен формироваться как: `https://post-ads.ru/api/deploy`

### Проверка .gitignore

Убедитесь, что в `.gitignore` есть:
```
*.rar
*.zip
*.7z
*.tar.gz
lagom-figma.rar
```

### Проверка больших файлов

При следующем деплое команда покажет предупреждение, если обнаружит большие файлы (>10MB).

---

## ⚠️ Если git push всё ещё медленный

Если у вас уже есть коммиты с большими файлами в истории:

1. **Удалите большие файлы из истории** (осторожно!):
```bash
git filter-branch --tree-filter 'rm -f lagom-figma.rar' HEAD
```

2. **Или используйте BFG Repo-Cleaner** (более безопасно):
```bash
# Скачайте BFG
# https://rtyley.github.io/bfg-repo-cleaner/
java -jar bfg.jar --delete-files lagom-figma.rar
git reflog expire --expire=now --all
git gc --prune=now --aggressive
```

3. **Или начните с чистого репозитория** (если возможно)

---

## ✅ После исправлений

После успешного push и деплоя, URL должен формироваться правильно:
```
📡 URL: https://post-ads.ru/api/deploy
```

А не дублироваться:
```
❌ https://post-ads.ru/api/deploy/api/deploy
```

