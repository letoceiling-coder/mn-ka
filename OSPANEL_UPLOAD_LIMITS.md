# Настройка лимитов загрузки файлов в OSPanel

## Быстрое решение

### Шаг 1: Найти php.ini в OSPanel

1. Откройте OSPanel
2. Откройте **Настройки** → **Модули** → **PHP**
3. Выберите версию PHP, которая используется (например, PHP 8.1, PHP 8.2)
4. Нажмите на иконку **📝 Редактировать php.ini**

Или найдите файл вручную:
- `C:\OSPanel\modules\php\PHP-8.x\php.ini`

### Шаг 2: Изменить настройки

Найдите и измените следующие строки:

```ini
upload_max_filesize = 100M
post_max_size = 100M
memory_limit = 256M
max_execution_time = 300
max_input_time = 300
```

**Важно:** `post_max_size` должен быть **больше или равен** `upload_max_filesize`

### Шаг 3: Перезапустить OSPanel

1. Нажмите **Остановить** в OSPanel
2. Нажмите **Запустить**

Или перезапустите только Apache:
- Нажмите правой кнопкой на Apache в OSPanel → **Перезапустить**

### Шаг 4: Проверить настройки

Создайте файл `test_limits.php` в корне проекта:

```php
<?php
phpinfo();
```

Откройте в браузере: `http://lagom-figma.loc/test_limits.php`

Найдите разделы:
- `upload_max_filesize` - должно быть `100M`
- `post_max_size` - должно быть `100M`
- `memory_limit` - должно быть `256M` или больше

## Настройки Apache (если нужно)

Если Apache все еще блокирует большие файлы, отредактируйте `.htaccess` в корне проекта:

```apache
php_value upload_max_filesize 100M
php_value post_max_size 100M
php_value memory_limit 256M
php_value max_execution_time 300
```

## Альтернативный способ (через консоль)

1. Откройте командную строку от имени администратора
2. Перейдите в папку OSPanel:
   ```cmd
   cd C:\OSPanel\modules\php\PHP-8.x
   ```
3. Откройте php.ini:
   ```cmd
   notepad php.ini
   ```
4. Измените значения как описано выше
5. Сохраните и перезапустите OSPanel

## Проверка через PHP CLI

```cmd
C:\OSPanel\modules\php\PHP-8.x\php.exe -i | findstr upload_max_filesize
C:\OSPanel\modules\php\PHP-8.x\php.exe -i | findstr post_max_size
```

## Если проблема сохраняется

1. Убедитесь, что вы изменили правильный php.ini (для версии PHP, которую использует Apache)
2. Проверьте, что Apache действительно перезапустился
3. Очистите кэш браузера
4. Проверьте логи OSPanel: `C:\OSPanel\logs\`
5. Проверьте, нет ли других ограничений в конфигурации Apache

## Рекомендуемые значения для разных размеров файлов

| Максимальный размер ZIP | upload_max_filesize | post_max_size | memory_limit |
|-------------------------|---------------------|---------------|--------------|
| 50 MB                   | 100M                | 100M          | 256M         |
| 100 MB                  | 100M                | 100M          | 256M         |
| 200 MB                  | 200M                | 200M          | 512M         |

## Примечание

После изменений **обязательно перезапустите Apache**, иначе изменения не вступят в силу!

