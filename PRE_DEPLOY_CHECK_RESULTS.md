# Результаты проверки проекта перед переносом на reg.ru

**Дата проверки:** 2026-01-18
**Команда:** `php artisan project:check`

## ✅ Результаты проверки

### 1. Миграции ✅
- **Статус:** ПРОЙДЕНО
- **Найдено миграций:** 62
- **Миграция add_protected_to_folders_table:** Исправлена (содержит проверку `Schema::hasColumn`)
- **Все критичные миграции:** На месте

### 2. Artisan команды ✅
- **Статус:** ПРОЙДЕНО
- **ProjectInstall:** ✅ Найдена
- **ImportSqlFile:** ✅ Найдена  
- **CreateUser:** ✅ Найдена

### 3. Конфигурационные файлы ✅
- **Статус:** ПРОЙДЕНО
- **.env.example:** ✅ Найден
- **composer.json:** ✅ Найден
- **package.json:** ✅ Найден
- **artisan:** ✅ Найден

### 4. SQL файл ⚠️
- **Статус:** ПРЕДУПРЕЖДЕНИЕ
- **Файл:** `dsc23ytp_lag_crm.sql`
- **Примечание:** Файл не найден в текущей директории проекта
- **Решение:** Убедитесь, что файл находится в корне проекта `C:\OSPanel\domains\mnka\` или укажите путь при установке

### 5. Структура директорий ✅
- **Статус:** ПРОЙДЕНО
- **Все необходимые директории:** На месте
  - app/
  - database/migrations/
  - database/seeders/
  - storage/ и storage/logs/
  - bootstrap/cache/
  - public/
  - config/

---

## 📊 Итоговый статус

**Ошибок:** 0 ❌  
**Предупреждений:** 1 ⚠️

### ⚠️ Предупреждение

SQL файл `dsc23ytp_lag_crm.sql` не найден в текущей директории проекта.  
**Действие:** Убедитесь, что файл находится в `C:\OSPanel\domains\mnka\dsc23ytp_lag_crm.sql`

---

## ✅ Проект готов к переносу!

Все критичные компоненты проверены и готовы к работе.

---

## 📋 Следующие шаги

### 1. Убедитесь, что SQL файл на месте:

```bash
# Проверьте в директории mnka
cd C:\OSPanel\domains\mnka
dir dsc23ytp_lag_crm.sql
```

### 2. Перенесите проект на reg.ru:

**Через панель управления или SCP/rsync:**

```bash
# На reg.ru после переноса файлов:
cd ~/mn-ka.ru
cp .env.example .env
nano .env  # Настройте БД
composer install --no-dev --optimize-autoloader
php artisan project:install --sql-file=dsc23ytp_lag_crm.sql --force
chmod -R 775 storage bootstrap/cache
```

### 3. Проверка на сервере:

```bash
php artisan migrate:status
php artisan project:check
```

---

## 📝 Рекомендации

1. **SQL файл:** Убедитесь, что `dsc23ytp_lag_crm.sql` включен в перенос
2. **.env файл:** Не переносите `.env`, создайте из `.env.example` на сервере
3. **vendor и node_modules:** Не переносите, установятся заново через composer/npm
4. **Права доступа:** После переноса установите `chmod -R 775 storage bootstrap/cache`

---

**Проверка выполнена:** ✅  
**Статус:** ГОТОВ К ПЕРЕНОСУ 🚀

