# Quiz Images Seeder - Инструкция

## Описание

Сидер `QuizImagesSeeder` создает изображения для квиза в медиа-библиотеке. Он может:
- Копировать изображения из текущего проекта (`public/img/delete/`)
- Копировать изображения из старого проекта (если путь указан)
- Создавать placeholder изображения (если исходные файлы не найдены)

## Использование

### 🎯 Простой способ (Рекомендуется)

**Просто скопируйте файлы в `public/img/delete/` текущего проекта!**

1. Создайте директорию `public/img/delete/` (если её нет)
2. Поместите туда следующие файлы:
   - `image-1.png` (для interest-1.jpg и interest-2.jpg)
   - `image-2.png` (для interest-3.jpg)
   - `image-3.png` (для interest-4.jpg)

3. Запустите сидер:
   ```bash
   php artisan db:seed --class=QuizImagesSeeder
   ```

Сидер автоматически:
- Найдет файлы в `public/img/delete/`
- Конвертирует PNG в JPG (если нужно)
- Скопирует их в `public/upload/quiz/`
- Зарегистрирует в медиа-библиотеке

### Локальная разработка (Windows)

1. **Способ 1** (рекомендуется): Поместите файлы в `public/img/delete/` текущего проекта

2. **Способ 2**: Если файлы находятся в старом проекте, сидер автоматически попытается найти их по пути `C:\OSPanel\domains\lagom`

3. Для указания другого пути к старому проекту, добавьте в `.env`:
   ```env
   OLD_PROJECT_PATH=C:\path\to\old\project
   ```

### Сервер (Linux)

1. **Способ 1** (рекомендуется): Поместите файлы в `public/img/delete/` на сервере

2. **Способ 2**: Если файлы находятся в старом проекте, добавьте в `.env`:
   ```env
   OLD_PROJECT_PATH=/path/to/old/project
   ```

3. **Способ 3**: Если исходных изображений нет, сидер автоматически создаст placeholder изображения

## Порядок поиска файлов

Сидер ищет файлы в следующем порядке:
1. ✅ **Сначала**: `public/img/delete/` в текущем проекте (приоритет)
2. Затем: Старый проект по пути из `OLD_PROJECT_PATH` (если указан)
3. Автоматические пути к старым проектам (если найдены)
4. Если ничего не найдено: создаются placeholder изображения

## Структура исходных файлов

Поместите следующие файлы в `public/img/delete/`:
- `image-1.png` - используется для "Купить/продать" и "Аренда земли"
- `image-2.png` - используется для "Юридические аспекты"
- `image-3.png` - используется для "Консультация"

## Что делает сидер

1. Создает директорию `public/upload/quiz/` (если не существует)
2. Пытается скопировать изображения из старого проекта
3. Если файлы не найдены, создает placeholder изображения с текстом
4. Регистрирует все изображения в медиа-библиотеке

## Результат

После выполнения сидера вы получите 4 изображения:
- `interest-1.jpg` - Купить/продать
- `interest-2.jpg` - Аренда земли
- `interest-3.jpg` - Юридические аспекты
- `interest-4.jpg` - Консультация

Все изображения будут доступны в медиа-библиотеке для использования в квизах.

## Замена placeholder изображений

Если placeholder изображения были созданы, вы можете позже:
1. Загрузить реальные изображения через админ-панель (Медиа)
2. Или запустить сидер снова после настройки `OLD_PROJECT_PATH`

## Примечание

Placeholder изображения имеют размер 245x140 пикселей и цвет фона #657C6C (основной цвет проекта).

