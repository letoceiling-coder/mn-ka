# Чек-лист обновления сервера для системы деплоя

## 📋 Что нужно обновить на сервере

### 1. Новые файлы (нужно загрузить)

Эти файлы были созданы и должны быть на сервере:

```
app/Http/Controllers/Api/DeployController.php
app/Http/Middleware/VerifyDeployToken.php
```

### 2. Измененные файлы (нужно обновить)

Эти файлы были изменены и должны быть обновлены на сервере:

```
routes/api.php                    # Добавлен маршрут POST /api/deploy
bootstrap/app.php                 # Добавлен middleware 'deploy.token'
.gitignore                        # Убрано исключение /public/build
```

### 3. Настройки .env (нужно добавить)

В файле `.env` на сервере нужно добавить:

```env
# Deploy Configuration
DEPLOY_TOKEN=your-secure-deploy-token-here-min-32-chars
PHP_PATH=php8.2
```

**⚠️ Важно:** `DEPLOY_TOKEN` должен быть **идентичным** на локальной машине и на сервере!

---

## 🚀 Пошаговая инструкция обновления

### Вариант 1: Ручное обновление (первый раз)

#### Шаг 1: Загрузите новые файлы

Скопируйте на сервер следующие файлы:

```bash
# На сервере
cd /path/to/your/project

# Создайте директории если их нет
mkdir -p app/Http/Controllers/Api
mkdir -p app/Http/Middleware

# Загрузите файлы (через FTP, SCP, или git)
# app/Http/Controllers/Api/DeployController.php
# app/Http/Middleware/VerifyDeployToken.php
```

#### Шаг 2: Обновите существующие файлы

Обновите следующие файлы:

```bash
# routes/api.php - добавьте маршрут /deploy
# bootstrap/app.php - добавьте middleware 'deploy.token'
# .gitignore - уберите /public/build
```

#### Шаг 3: Настройте .env

Откройте `.env` на сервере и добавьте:

```env
DEPLOY_TOKEN=your-secure-deploy-token-here-min-32-chars
PHP_PATH=php8.2
```

**Где взять токен?**
- Используйте тот же токен, что указан в `.env` на локальной машине
- Или сгенерируйте новый: `openssl rand -hex 32`

**Как определить PHP_PATH?**
```bash
which php8.2
# или
which php8.3
# или
which php
```

#### Шаг 4: Очистите кеши

```bash
php artisan config:clear
php artisan route:clear
php artisan cache:clear
php artisan optimize:clear
```

#### Шаг 5: Обновите автозагрузку классов

```bash
composer dump-autoload
```

#### Шаг 6: Проверьте работу

```bash
# Проверьте, что маршрут доступен
php artisan route:list | grep deploy

# Должно показать:
# POST      api/deploy ................... deploy
```

---

### Вариант 2: Через Git (рекомендуется)

Если вы используете Git, то после коммита и пуша на сервере выполните:

```bash
# На сервере
cd /path/to/your/project

# Получите обновления
git pull origin main

# Установите зависимости (если были изменения в composer.json)
composer install --no-dev --optimize-autoloader

# Выполните миграции (если были новые)
php artisan migrate --force

# Очистите кеши
php artisan config:clear
php artisan route:clear
php artisan cache:clear
php artisan optimize:clear

# Обновите автозагрузку
composer dump-autoload

# Оптимизируйте приложение
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

**⚠️ Важно:** После `git pull` обязательно настройте `.env` (добавьте `DEPLOY_TOKEN` и `PHP_PATH`).

---

## ✅ Проверка после обновления

### 1. Проверка файлов

Убедитесь, что файлы на месте:

```bash
# Проверьте наличие новых файлов
ls -la app/Http/Controllers/Api/DeployController.php
ls -la app/Http/Middleware/VerifyDeployToken.php

# Проверьте маршруты
php artisan route:list | grep deploy
```

### 2. Проверка .env

```bash
# Проверьте наличие переменных
grep DEPLOY_TOKEN .env
grep PHP_PATH .env
```

### 3. Тест API endpoint

```bash
# Тест с правильным токеном
curl -X POST https://your-domain.com/api/deploy \
  -H "X-Deploy-Token: your-deploy-token" \
  -H "Content-Type: application/json"

# Должен вернуть JSON с результатами деплоя или ошибку
```

### 4. Проверка логов

```bash
# Проверьте логи на наличие ошибок
tail -f storage/logs/laravel.log
```

---

## 🔧 Дополнительные настройки (опционально)

### Настройка прав доступа

Убедитесь, что пользователь веб-сервера имеет права:

```bash
# Проверьте пользователя веб-сервера
ps aux | grep -E 'nginx|apache|httpd' | head -1

# Установите права (замените USER на пользователя веб-сервера)
sudo chown -R USER:USER /path/to/your/project
sudo chmod -R 755 /path/to/your/project
sudo chmod -R 775 /path/to/your/project/storage
sudo chmod -R 775 /path/to/your/project/bootstrap/cache
```

### Настройка Git для деплоя

Для выполнения `git pull` на сервере:

```bash
# Настройте SSH ключи или credentials
sudo -u www-data git config credential.helper store
```

---

## ⚠️ Важные замечания

1. **Токен безопасности:**
   - `DEPLOY_TOKEN` должен быть одинаковым на локальной машине и на сервере
   - Используйте сложный токен (минимум 32 символа)
   - Храните токен только в `.env` (не коммитьте в Git)

2. **PHP путь:**
   - Убедитесь, что указанный `PHP_PATH` доступен для пользователя веб-сервера
   - Проверьте: `which php8.2` или `php8.2 --version`

3. **Права доступа:**
   - Пользователь веб-сервера должен иметь права на выполнение `git`, `composer`, `php`
   - Проверьте права на запись в директорию проекта

4. **База данных:**
   - Убедитесь, что миграции могут выполняться
   - Проверьте: `php artisan migrate:status`

---

## 🐛 Решение проблем

### Проблема: "Class DeployController does not exist"

**Решение:**
```bash
composer dump-autoload
php artisan config:clear
```

### Проблема: "Route [deploy] not defined"

**Решение:**
```bash
php artisan route:clear
php artisan route:cache
```

### Проблема: "DEPLOY_TOKEN не настроен на сервере"

**Решение:**
Добавьте `DEPLOY_TOKEN` в `.env` на сервере и выполните:
```bash
php artisan config:clear
```

### Проблема: "Permission denied" при git pull

**Решение:**
Настройте права доступа и SSH ключи для пользователя веб-сервера (см. раздел "Настройка Git для деплоя").

### Проблема: "detected dubious ownership in repository"

**Решение:**
Эта ошибка возникает, когда владелец репозитория отличается от пользователя, выполняющего команду. Выполните на сервере:

```bash
# На сервере, в директории проекта
cd /path/to/your/project

# Добавьте директорию в безопасные (глобально)
git config --global --add safe.directory /path/to/your/project

# Или локально (если нет прав на глобальную конфигурацию)
git config --local --add safe.directory /path/to/your/project

# Замените /path/to/your/project на реальный путь, например:
# git config --global --add safe.directory /home/d/dsc23ytp/stroy/public_html
```

**Примечание:** Система деплоя автоматически пытается настроить это при каждом запуске, но для надежности лучше настроить вручную один раз.

---

## 📝 Краткая версия (для быстрого обновления)

```bash
# 1. Обновите код (через git или вручную)
git pull origin main

# 2. Установите зависимости
composer install --no-dev --optimize-autoloader

# 3. Добавьте в .env:
# DEPLOY_TOKEN=your-token
# PHP_PATH=php8.2

# 4. Очистите кеши
php artisan optimize:clear

# 5. Обновите автозагрузку
composer dump-autoload

# 6. Оптимизируйте
php artisan config:cache
php artisan route:cache
php artisan view:cache

# 7. Проверьте
php artisan route:list | grep deploy
```

---

**После выполнения всех шагов система деплоя будет готова к работе!**

